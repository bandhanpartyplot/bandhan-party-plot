/* =========================================================
   BANDHAN PARTY PLOT — Stylesheet
   Palette: deep green / warm gold / ivory / maroon accent
   ========================================================= */

:root{
  --ivory:#FBF6EC;
  --cream:#F2EAD6;
  --green-900:#0E241A;
  --green-800:#15332A;
  --green-700:#1C3E30;
  --gold:#C6A15B;
  --gold-light:#E7D2A0;
  --maroon:#7C1F2B;
  --maroon-dark:#5E1620;
  --ink:#241E15;
  --ink-soft:#5B5142;
  --line: rgba(198,161,91,0.35);
  --ok:#3F7D53;
  --warn:#C08A2E;
  --danger:#B23A3A;
  --shadow: 0 25px 60px -25px rgba(14,36,26,0.45);
  --ease: cubic-bezier(.22,.61,.36,1);
}
*{box-sizing:border-box;}
html{scroll-behavior:smooth;}
body{
  margin:0;
  font-family:'Jost',sans-serif;
  background:var(--ivory);
  color:var(--ink);
  -webkit-font-smoothing:antialiased;
  overflow-x:hidden;
}
img{max-width:100%; display:block;}
a{color:inherit; text-decoration:none;}
h1,h2,h3,h4{font-family:'Marcellus',serif; margin:0; font-weight:400; letter-spacing:.01em;}
.quote-font{font-family:'Cormorant Garamond', serif;}
p{line-height:1.75; color:var(--ink-soft); margin:0;}
.wrap{max-width:1180px; margin:0 auto; padding:0 24px;}
.eyebrow{
  font-family:'Jost',sans-serif; text-transform:uppercase; letter-spacing:.32em; font-size:.72rem;
  color:var(--maroon); font-weight:600; display:flex; align-items:center; gap:14px; margin-bottom:18px;
}
.eyebrow::before{content:""; width:34px; height:1px; background:var(--gold);}
section{position:relative;}

/* ---------- Buttons ---------- */
.btn{
  display:inline-flex; align-items:center; justify-content:center; gap:10px;
  padding:16px 30px; border-radius:2px; font-size:.86rem; letter-spacing:.06em; text-transform:uppercase;
  font-weight:600; cursor:pointer; border:1px solid transparent; transition:transform .4s var(--ease), box-shadow .4s var(--ease), background .3s, color .3s, border-color .3s;
  white-space:nowrap; font-family:'Jost',sans-serif;
}
.btn svg{width:17px; height:17px; flex:none;}
.btn-gold{ background:var(--gold); color:var(--green-900); }
.btn-gold:hover{ transform:translateY(-3px); box-shadow:0 18px 30px -14px rgba(198,161,91,.65); }
.btn-outline{ background:transparent; color:var(--ivory); border-color:rgba(251,246,236,.55); }
.btn-outline:hover{ background:rgba(251,246,236,.12); border-color:var(--ivory); transform:translateY(-3px); }
.btn-maroon{ background:var(--maroon); color:var(--ivory); }
.btn-maroon:hover{ background:var(--maroon-dark); transform:translateY(-3px); box-shadow:0 18px 30px -14px rgba(124,31,43,.55);}
.btn-line{ background:transparent; color:var(--green-800); border-color:var(--green-800); }
.btn-line:hover{ background:var(--green-800); color:var(--ivory); }
.btn-sm{ padding:10px 18px; font-size:.72rem; }
.btn[disabled]{ opacity:.45; cursor:not-allowed; transform:none !important; box-shadow:none !important; }

/* ---------- Toran / bandhanwar divider (signature element) ---------- */
.toran{ width:100%; overflow:hidden; line-height:0; background:var(--ivory); }
.toran svg{ width:100%; height:auto; display:block; }
.toran.dark{ background:var(--green-900); }

/* ---------- Header / Nav ---------- */
header{
  position:fixed; top:0; left:0; right:0; z-index:200;
  background:linear-gradient(to bottom, rgba(14,36,26,.9), rgba(14,36,26,0));
  padding:20px 0 40px; transition:background .4s, padding .4s, box-shadow .4s;
}
header.scrolled{ background:rgba(14,36,26,.97); padding:12px 0; box-shadow:0 10px 30px rgba(0,0,0,.25); backdrop-filter:blur(6px); }
header.solid{ background:var(--green-900); padding:14px 0; }
nav{ display:flex; align-items:center; justify-content:space-between; max-width:1180px; margin:0 auto; padding:0 24px; }
.brand{ display:flex; align-items:center; gap:12px; color:var(--ivory); }
.brand-mark{ width:42px; height:42px; border:1px solid var(--gold); border-radius:50%; display:flex; align-items:center; justify-content:center; font-family:'Marcellus',serif; color:var(--gold); font-size:1.2rem; flex:none;}
.brand-text{ display:flex; flex-direction:column; line-height:1.15; }
.brand-text b{ font-family:'Marcellus',serif; font-size:1.08rem; letter-spacing:.06em; }
.brand-text span{ font-size:.62rem; letter-spacing:.28em; color:var(--gold-light); text-transform:uppercase; }
.nav-links{ display:flex; gap:32px; list-style:none; margin:0; padding:0; }
.nav-links a{ color:var(--ivory); font-size:.86rem; letter-spacing:.03em; position:relative; padding-bottom:4px;}
.nav-links a.active,
.nav-links a:hover{ color:var(--gold-light); }
.nav-links a::after{ content:""; position:absolute; left:0; bottom:0; width:0; height:1px; background:var(--gold); transition:width .35s var(--ease); }
.nav-links a:hover::after{ width:100%; }
.nav-cta{ display:flex; gap:12px; align-items:center; }
.nav-cta .btn{ padding:11px 20px; font-size:.72rem; }
.burger{ display:none; flex-direction:column; gap:5px; cursor:pointer; background:none; border:none; padding:6px; }
.burger span{ width:24px; height:2px; background:var(--ivory); display:block; }
.mobile-drawer{ display:none; position:fixed; inset:0; z-index:199; background:rgba(14,36,26,.98); color:var(--ivory); flex-direction:column; align-items:center; justify-content:center; gap:28px; font-family:'Marcellus',serif; font-size:1.35rem; }

/* ---------- reveal-on-scroll ---------- */
.reveal{ opacity:0; transform:translateY(34px); transition:opacity .9s var(--ease), transform .9s var(--ease); }
.reveal.in{ opacity:1; transform:translateY(0); }
.stagger .reveal:nth-child(1){transition-delay:.05s;}
.stagger .reveal:nth-child(2){transition-delay:.15s;}
.stagger .reveal:nth-child(3){transition-delay:.25s;}
.stagger .reveal:nth-child(4){transition-delay:.35s;}
.stagger .reveal:nth-child(5){transition-delay:.45s;}
.stagger .reveal:nth-child(6){transition-delay:.55s;}

/* ---------- Section head ---------- */
.section-head{ max-width:640px; margin-bottom:56px; }
.section-head.center{ margin-left:auto; margin-right:auto; text-align:center; }
.section-head.center .eyebrow{ justify-content:center; }
.section-head.center .eyebrow::before{ display:none; }
.section-head h2{ font-size:clamp(1.9rem,3.6vw,2.7rem); color:var(--green-900); }
.section-head p{ margin-top:18px; font-size:1.05rem; }
.light .section-head h2{ color:var(--ivory); }
.light .section-head p{ color:rgba(251,246,236,.78); }
.light .eyebrow{ color:var(--gold-light); }

/* ---------- Page hero (interior pages) ---------- */
.page-hero{
  padding:170px 0 90px; background:var(--green-900); color:var(--ivory); position:relative; overflow:hidden;
  background-image: radial-gradient(circle at 15% 25%, rgba(198,161,91,.14), transparent 40%), radial-gradient(circle at 85% 75%, rgba(124,31,43,.18), transparent 45%);
}
.page-hero .eyebrow{ justify-content:center; color:var(--gold-light); }
.page-hero .eyebrow::before{ display:none; }
.page-hero h1{ text-align:center; font-size:clamp(2.1rem,4.4vw,3.2rem); }
.page-hero p.lead{ text-align:center; max-width:600px; margin:20px auto 0; color:rgba(251,246,236,.82); font-family:'Cormorant Garamond',serif; font-size:1.25rem; }
.breadcrumb{ text-align:center; font-size:.78rem; letter-spacing:.08em; text-transform:uppercase; color:rgba(251,246,236,.55); margin-top:16px; }
.breadcrumb a{ color:var(--gold-light); }

/* ---------- Hero (home) ---------- */
.hero{
  min-height:100svh; position:relative; display:flex; align-items:center;
  background:var(--green-900); color:var(--ivory); overflow:hidden;
}
.hero-pattern{ position:absolute; inset:0; opacity:.5; }
.hero-glow{ position:absolute; width:900px; height:900px; border-radius:50%; filter:blur(120px); opacity:.22; }
.hero-glow.g1{ background:var(--gold); top:-320px; left:-260px; }
.hero-glow.g2{ background:var(--maroon); bottom:-360px; right:-260px; }
.hero-inner{ max-width:820px; margin:0 auto; text-align:center; padding:170px 24px 130px; position:relative; z-index:2; }
.hero .eyebrow{ justify-content:center; color:var(--gold-light); }
.hero .eyebrow::before{ display:none; }
.hero h1{ font-size:clamp(2.4rem, 5.6vw, 4.3rem); line-height:1.14; color:var(--ivory); }
.hero h1 em{ font-style:italic; color:var(--gold-light); }
.hero p.sub{ margin:26px auto 0; max-width:560px; font-family:'Cormorant Garamond',serif; font-size:1.35rem; color:rgba(251,246,236,.88); }
.hero-btns{ display:flex; gap:16px; justify-content:center; flex-wrap:wrap; margin-top:44px; }
.hero-scroll{ position:absolute; bottom:34px; left:50%; transform:translateX(-50%); color:var(--gold-light); display:flex; flex-direction:column; align-items:center; gap:8px; font-size:.68rem; letter-spacing:.3em; text-transform:uppercase; opacity:.85; z-index:2;}
.hero-scroll .line{ width:1px; height:38px; background:linear-gradient(var(--gold), transparent); animation:scrollLine 2.2s infinite var(--ease); }
@keyframes scrollLine{ 0%{transform:scaleY(0); transform-origin:top;} 50%{transform:scaleY(1); transform-origin:top;} 50.01%{transform-origin:bottom;} 100%{transform:scaleY(0); transform-origin:bottom;} }

/* ---------- About ---------- */
.about{ padding:120px 0 100px; background:var(--ivory); }
.about-grid{ display:grid; grid-template-columns:.95fr 1.05fr; gap:70px; align-items:center; }
.photo-slot{
  position:relative; border-radius:4px; box-shadow:var(--shadow); width:100%; height:520px;
  background:
    repeating-linear-gradient(135deg, rgba(198,161,91,.10) 0 2px, transparent 2px 14px),
    linear-gradient(160deg, var(--green-800), var(--green-900));
  display:flex; flex-direction:column; align-items:center; justify-content:center; gap:14px; color:var(--gold-light);
  border:1px dashed rgba(198,161,91,.5);
  text-align:center; padding:20px;
}
.photo-slot svg{ width:44px; height:44px; opacity:.8; }
.photo-slot span{ font-size:.72rem; letter-spacing:.14em; text-transform:uppercase; color:rgba(231,210,160,.75); max-width:220px; }
.about-media{ position:relative; }
.about-media .frame{ position:absolute; inset:22px -22px -22px 22px; border:1px solid var(--gold); z-index:-1; border-radius:4px; }
.about-badge{
  position:absolute; left:-30px; bottom:-30px; background:var(--green-900); color:var(--ivory);
  padding:26px 30px; text-align:center; box-shadow:var(--shadow); border:1px solid var(--gold);
}
.about-badge b{ font-family:'Marcellus',serif; font-size:1.7rem; color:var(--gold-light); display:block; }
.about-badge span{ font-size:.66rem; letter-spacing:.16em; text-transform:uppercase; color:rgba(251,246,236,.75); }
.about-copy h2{ font-size:clamp(1.9rem,3.4vw,2.6rem); color:var(--green-900); margin-bottom:22px; }
.about-copy p + p{ margin-top:16px; }
.about-list{ list-style:none; margin:30px 0 0; padding:0; display:grid; grid-template-columns:1fr 1fr; gap:14px 24px; }
.about-list li{ display:flex; align-items:center; gap:10px; font-size:.92rem; color:var(--ink); }
.about-list svg{ width:16px; height:16px; color:var(--maroon); flex:none; }

/* ---------- Highlights ---------- */
.highlights{ padding:110px 0; background:var(--cream); }
.h-grid{ display:grid; grid-template-columns:repeat(3, 1fr); gap:28px; }
.h-card{
  background:var(--ivory); padding:40px 30px 34px; border:1px solid var(--line); position:relative; overflow:hidden;
  transition:transform .5s var(--ease), box-shadow .5s var(--ease), border-color .5s;
}
.h-card:hover{ transform:translateY(-8px); box-shadow:var(--shadow); border-color:var(--gold); }
.h-card .num{ font-family:'Cormorant Garamond',serif; font-style:italic; font-size:.95rem; color:var(--gold); letter-spacing:.05em;}
.h-icon{ width:52px; height:52px; border-radius:50%; background:var(--green-900); display:flex; align-items:center; justify-content:center; margin:16px 0 22px; transition:background .4s;}
.h-card:hover .h-icon{ background:var(--maroon); }
.h-icon svg{ width:24px; height:24px; color:var(--gold-light); }
.h-card h3{ font-size:1.28rem; color:var(--green-900); margin-bottom:10px; }
.h-card p{ font-size:.92rem; }

/* ---------- Amenities ---------- */
.amenities{ padding:110px 0; background:var(--green-900); color:var(--ivory); position:relative; }
.amenities::before{
  content:""; position:absolute; inset:0; opacity:.08; pointer-events:none;
  background-image:radial-gradient(circle at 1px 1px, var(--gold) 1px, transparent 0);
  background-size:26px 26px;
}
.am-cols{ display:grid; grid-template-columns:1fr 1fr; gap:60px; position:relative; }
.am-block h3{ color:var(--gold-light); font-size:1.3rem; margin-bottom:26px; display:flex; align-items:center; gap:12px; }
.am-list{ list-style:none; margin:0; padding:0; display:flex; flex-direction:column; gap:16px; }
.am-list li{ display:flex; gap:14px; align-items:center; padding:16px 18px; border:1px solid rgba(198,161,91,.28); transition:background .35s, transform .35s var(--ease); }
.am-list li:hover{ background:rgba(198,161,91,.08); transform:translateX(6px); }
.am-list .ic{ width:38px; height:38px; border-radius:50%; border:1px solid var(--gold); display:flex; align-items:center; justify-content:center; flex:none; }
.am-list .ic svg{ width:18px; height:18px; color:var(--gold-light); }
.am-list p{ color:rgba(251,246,236,.9); font-size:.94rem; margin:0; }

/* ---------- Gallery ---------- */
.gallery{ padding:110px 0 120px; background:var(--ivory); }
.g-grid{ display:grid; grid-template-columns:repeat(4, 1fr); grid-auto-rows:150px; gap:14px; }
.g-item{ position:relative; overflow:hidden; border-radius:2px; }
.g-item.tall{ grid-row:span 2; }
.g-item.wide{ grid-column:span 2; }

/* ---------- Location ---------- */
.location{ padding:110px 0; background:var(--cream); }
.loc-grid{ display:grid; grid-template-columns:1fr 1fr; gap:0; box-shadow:var(--shadow); }
.loc-map iframe{ width:100%; height:460px; border:0; filter:saturate(.85); }
.loc-info{ background:var(--green-900); color:var(--ivory); padding:52px 46px; display:flex; flex-direction:column; justify-content:center; gap:22px; }
.loc-info h3{ font-size:1.7rem; color:var(--gold-light); }
.loc-row{ display:flex; gap:16px; align-items:flex-start; }
.loc-row svg{ width:20px; height:20px; color:var(--gold); flex:none; margin-top:3px; }
.loc-row p{ color:rgba(251,246,236,.85); }
.loc-info .btn{ align-self:flex-start; margin-top:10px; }

/* ---------- Contact ---------- */
.contact{ padding:120px 0; background:var(--ivory); }
.c-grid{ display:grid; grid-template-columns:1.15fr .85fr; gap:0; box-shadow:var(--shadow); }
.c-form{ background:#fff; padding:54px 50px; border:1px solid var(--line); border-right:none; }
.c-form h3{ font-size:1.6rem; color:var(--green-900); margin-bottom:8px; }
.c-form > p{ margin-bottom:30px; }
.field-row{ display:grid; grid-template-columns:1fr 1fr; gap:18px; }
.field{ margin-bottom:18px; }
.field label{ display:block; font-size:.72rem; letter-spacing:.12em; text-transform:uppercase; color:var(--ink-soft); margin-bottom:8px; }
.field input, .field select, .field textarea{
  width:100%; border:1px solid #ddd3ba; background:var(--ivory); padding:13px 14px; font-family:'Jost',sans-serif;
  font-size:.94rem; color:var(--ink); transition:border-color .3s, box-shadow .3s; border-radius:2px;
}
.field input:focus, .field select:focus, .field textarea:focus{ outline:none; border-color:var(--gold); box-shadow:0 0 0 3px rgba(198,161,91,.18); }
.c-form button{ width:100%; margin-top:6px; border:none; }
.c-side{ background:var(--green-900); color:var(--ivory); padding:54px 44px; display:flex; flex-direction:column; gap:26px; position:relative; }
.c-side h3{ font-size:1.5rem; color:var(--gold-light); }
.c-item{ display:flex; gap:14px; align-items:flex-start; }
.c-item svg{ width:19px; height:19px; color:var(--gold); flex:none; margin-top:2px; }
.c-item a, .c-item span{ color:rgba(251,246,236,.9); font-size:.94rem; }
.c-item a:hover{ color:var(--gold-light); }
.c-side .btn{ margin-top:6px; }
.wa-btn{ background:#25D366; color:#fff; }
.wa-btn:hover{ background:#1ebd5a; transform:translateY(-3px); }
.form-note{ font-size:.78rem; color:var(--ink-soft); margin-top:14px; }
.form-success{ display:none; padding:16px 18px; background:rgba(63,125,83,.12); border:1px solid var(--ok); color:#2c5a3a; font-size:.88rem; margin-bottom:18px; }
.form-success.show{ display:block; }

/* ---------- Footer ---------- */
footer{ background:var(--green-900); color:rgba(251,246,236,.75); }
.footer-top{ padding:70px 0 50px; display:grid; grid-template-columns:1.4fr 1fr 1fr 1.1fr; gap:40px; border-top:1px solid rgba(198,161,91,.2); }
.footer-brand .brand{ margin-bottom:16px; }
.footer-brand p{ color:rgba(251,246,236,.65); font-size:.92rem; max-width:280px; }
.footer-social{ display:flex; gap:12px; margin-top:20px; }
.footer-social a{ width:38px; height:38px; border:1px solid rgba(198,161,91,.4); border-radius:50%; display:flex; align-items:center; justify-content:center; transition:all .3s; }
.footer-social a:hover{ background:var(--gold); border-color:var(--gold); color:var(--green-900); }
.footer-social svg{ width:16px; height:16px; }
footer h4{ font-family:'Jost',sans-serif; font-weight:600; letter-spacing:.1em; text-transform:uppercase; font-size:.78rem; color:var(--gold-light); margin-bottom:20px; }
.footer-links{ list-style:none; padding:0; margin:0; display:flex; flex-direction:column; gap:12px; }
.footer-links a{ font-size:.92rem; color:rgba(251,246,236,.72); transition:color .3s, padding-left .3s; }
.footer-links a:hover{ color:var(--gold-light); padding-left:4px; }
.footer-contact li{ display:flex; gap:10px; align-items:flex-start; font-size:.9rem; margin-bottom:14px; list-style:none; }
.footer-contact svg{ width:16px; height:16px; color:var(--gold); flex:none; margin-top:3px; }
.footer-bottom{ border-top:1px solid rgba(198,161,91,.16); padding:22px 0; display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:10px; font-size:.8rem; color:rgba(251,246,236,.5); }

/* ---------- Floating buttons ---------- */
.float-stack{ position:fixed; right:22px; bottom:22px; z-index:300; display:flex; flex-direction:column; gap:14px; align-items:flex-end; }
.fab{ width:58px; height:58px; border-radius:50%; display:flex; align-items:center; justify-content:center; box-shadow:0 14px 30px -8px rgba(0,0,0,.4); transition:transform .35s var(--ease); }
.fab:hover{ transform:scale(1.08); }
.fab.wa{ background:#25D366; color:#fff; animation:pulse 2.4s infinite; }
.fab.call{ background:var(--gold); color:var(--green-900); width:50px; height:50px; }
.fab svg{ width:26px; height:26px; }
.fab.call svg{ width:22px; height:22px; }
@keyframes pulse{ 0%{box-shadow:0 0 0 0 rgba(37,211,102,.55);} 70%{box-shadow:0 0 0 16px rgba(37,211,102,0);} 100%{box-shadow:0 0 0 0 rgba(37,211,102,0);} }
.mobile-bar{ display:none; }

/* =========================================================
   BOOKING SYSTEM
   ========================================================= */
.booking-shell{ padding:80px 0 120px; background:var(--ivory); }
.booking-layout{ display:grid; grid-template-columns:1.4fr .9fr; gap:34px; align-items:flex-start; }

.cal-card{ background:#fff; border:1px solid var(--line); box-shadow:var(--shadow); }
.cal-head{ display:flex; align-items:center; justify-content:space-between; padding:26px 28px; background:var(--green-900); color:var(--ivory); }
.cal-head h3{ font-size:1.35rem; color:var(--gold-light); }
.cal-nav{ display:flex; gap:8px; }
.cal-nav button{ width:38px; height:38px; border-radius:50%; border:1px solid rgba(198,161,91,.5); background:transparent; color:var(--ivory); cursor:pointer; display:flex; align-items:center; justify-content:center; transition:background .3s; }
.cal-nav button:hover{ background:rgba(198,161,91,.18); }
.cal-nav svg{ width:16px; height:16px; }
.cal-legend{ display:flex; gap:18px; flex-wrap:wrap; padding:18px 28px; border-bottom:1px solid var(--line); background:var(--cream); }
.leg-item{ display:flex; align-items:center; gap:8px; font-size:.78rem; color:var(--ink-soft); letter-spacing:.02em; }
.leg-dot{ width:11px; height:11px; border-radius:3px; }
.leg-dot.available{ background:var(--ok); }
.leg-dot.partial{ background:var(--warn); }
.leg-dot.booked{ background:var(--danger); }
.leg-dot.selected{ background:var(--gold); }
.leg-dot.past{ background:#cfc7b3; }

.cal-grid{ padding:24px 24px 30px; }
.cal-weekdays{ display:grid; grid-template-columns:repeat(7,1fr); gap:8px; margin-bottom:10px; }
.cal-weekdays span{ text-align:center; font-size:.68rem; letter-spacing:.1em; text-transform:uppercase; color:var(--ink-soft); }
.cal-days{ display:grid; grid-template-columns:repeat(7,1fr); gap:8px; }
.day-cell{
  aspect-ratio:1/1; border:1px solid var(--line); border-radius:4px; display:flex; flex-direction:column;
  align-items:center; justify-content:center; gap:5px; cursor:pointer; background:var(--ivory);
  transition:transform .25s var(--ease), box-shadow .25s var(--ease), border-color .25s; position:relative;
}
.day-cell .dnum{ font-family:'Marcellus',serif; font-size:1rem; color:var(--ink); }
.day-cell .dstatus{ width:7px; height:7px; border-radius:50%; }
.day-cell.available .dstatus{ background:var(--ok); }
.day-cell.partial .dstatus{ background:var(--warn); }
.day-cell.booked .dstatus{ background:var(--danger); }
.day-cell.empty{ visibility:hidden; cursor:default; }
.day-cell.past{ opacity:.4; cursor:not-allowed; }
.day-cell.past .dstatus{ background:#cfc7b3; }
.day-cell:not(.empty):not(.past):hover{ transform:translateY(-3px); box-shadow:0 12px 22px -12px rgba(14,36,26,.35); border-color:var(--gold); }
.day-cell.selected{ background:var(--gold); border-color:var(--gold); }
.day-cell.selected .dnum{ color:var(--green-900); }
.day-cell.today{ box-shadow:inset 0 0 0 1px var(--maroon); }

.slot-panel{ position:sticky; top:110px; background:var(--green-900); color:var(--ivory); padding:36px 32px; box-shadow:var(--shadow); }
.slot-panel h3{ color:var(--gold-light); font-size:1.3rem; }
.slot-panel .sel-date{ font-family:'Cormorant Garamond',serif; font-size:1.1rem; color:rgba(251,246,236,.85); margin-top:6px; }
.slot-list{ list-style:none; margin:26px 0 0; padding:0; display:flex; flex-direction:column; gap:12px; }
.slot-list li{ display:flex; justify-content:space-between; align-items:center; padding:14px 16px; border:1px solid rgba(198,161,91,.3); border-radius:3px; }
.slot-list .s-name{ font-size:.92rem; }
.slot-list .s-name small{ display:block; color:rgba(251,246,236,.55); font-size:.7rem; margin-top:2px;}
.tag{ font-size:.66rem; letter-spacing:.1em; text-transform:uppercase; padding:5px 10px; border-radius:20px; font-weight:600; }
.tag.available{ background:rgba(63,125,83,.22); color:#8fd6a4; }
.tag.booked{ background:rgba(178,58,58,.22); color:#f0a3a3; }
.slot-empty-hint{ margin-top:24px; padding:18px; border:1px dashed rgba(198,161,91,.4); font-size:.85rem; color:rgba(251,246,236,.7); text-align:center; }
.slot-panel .btn{ width:100%; margin-top:26px; }
.slot-panel .btn + .btn{ margin-top:12px; }

/* Booking request modal */
.modal-overlay{ display:none; position:fixed; inset:0; background:rgba(14,36,26,.72); z-index:400; align-items:center; justify-content:center; padding:20px; backdrop-filter:blur(3px); }
.modal-overlay.show{ display:flex; }
.modal-box{ background:#fff; max-width:520px; width:100%; padding:40px; box-shadow:var(--shadow); position:relative; max-height:90vh; overflow-y:auto; }
.modal-close{ position:absolute; top:16px; right:16px; width:34px; height:34px; border-radius:50%; border:1px solid var(--line); background:none; cursor:pointer; display:flex; align-items:center; justify-content:center; }
.modal-close svg{ width:16px; height:16px; }
.modal-box h3{ font-size:1.4rem; color:var(--green-900); margin-bottom:6px; }
.modal-box p.sub-note{ font-size:.86rem; margin-bottom:22px; }

/* Admin */
.admin-toggle{ position:fixed; left:18px; bottom:18px; z-index:150; font-size:.68rem; letter-spacing:.08em; text-transform:uppercase; color:var(--ink-soft); background:var(--cream); border:1px solid var(--line); padding:8px 12px; border-radius:20px; cursor:pointer; opacity:.55; transition:opacity .3s; }
.admin-toggle:hover{ opacity:1; }
.admin-panel{ background:var(--cream); border:1px solid var(--line); margin-top:40px; padding:30px 28px; }
.admin-panel.hidden{ display:none; }
.admin-panel h3{ color:var(--green-900); font-size:1.2rem; margin-bottom:6px; }
.admin-row{ display:grid; grid-template-columns:1fr 1fr 1fr auto; gap:12px; margin-top:20px; align-items:end; }
.admin-row .field{ margin-bottom:0; }
.admin-list{ margin-top:24px; border-top:1px solid var(--line); padding-top:18px; }
.admin-entry{ display:flex; justify-content:space-between; align-items:center; padding:10px 0; border-bottom:1px dashed var(--line); font-size:.85rem; }
.admin-entry button{ background:none; border:none; color:var(--maroon); cursor:pointer; font-size:.78rem; text-decoration:underline; }
.lock-screen{ text-align:center; padding:40px 20px; }
.lock-screen input{ max-width:220px; margin:16px auto; text-align:center; }

/* ---------- Admin dashboard v2 ---------- */
.admin-top{ display:flex; justify-content:space-between; align-items:flex-start; flex-wrap:wrap; gap:16px; }
.sync-badge{ display:inline-flex; align-items:center; gap:8px; font-size:.72rem; letter-spacing:.06em; text-transform:uppercase; padding:7px 13px; border-radius:20px; font-weight:600; }
.sync-badge.local{ background:rgba(192,138,46,.15); color:#8a611f; }
.sync-badge.live{ background:rgba(63,125,83,.15); color:#2c5a3a; }
.sync-badge .dot{ width:7px; height:7px; border-radius:50%; background:currentColor; }
.admin-tabs{ display:flex; gap:10px; margin-top:24px; flex-wrap:wrap; }
.admin-tab{ padding:9px 18px; border:1px solid var(--line); background:#fff; font-size:.78rem; letter-spacing:.04em; text-transform:uppercase; cursor:pointer; border-radius:20px; color:var(--ink-soft); transition:all .25s; }
.admin-tab.active{ background:var(--green-900); color:var(--ivory); border-color:var(--green-900); }
.admin-tab .count{ display:inline-block; margin-left:6px; background:rgba(0,0,0,.12); border-radius:10px; padding:1px 7px; font-size:.7rem; }
.admin-tab.active .count{ background:rgba(255,255,255,.18); }
.booking-card{ background:#fff; border:1px solid var(--line); border-radius:3px; padding:18px 20px; margin-top:14px; display:flex; justify-content:space-between; gap:16px; align-items:flex-start; flex-wrap:wrap; }
.bk-info{ display:flex; flex-direction:column; gap:4px; min-width:220px; }
.bk-date{ font-family:'Marcellus',serif; font-size:1.05rem; color:var(--green-900); }
.bk-meta{ font-size:.82rem; color:var(--ink-soft); }
.bk-meta b{ color:var(--ink); font-weight:600; }
.bk-actions{ display:flex; gap:8px; flex-wrap:wrap; align-items:center; }
.bk-actions button{ border:1px solid var(--line); background:#fff; padding:8px 14px; font-size:.72rem; text-transform:uppercase; letter-spacing:.04em; cursor:pointer; border-radius:20px; transition:all .2s; }
.bk-actions .confirm{ color:#2c5a3a; border-color:var(--ok); }
.bk-actions .confirm:hover{ background:var(--ok); color:#fff; }
.bk-actions .decline, .bk-actions .cancel{ color:var(--maroon); border-color:var(--maroon); }
.bk-actions .decline:hover, .bk-actions .cancel:hover{ background:var(--maroon); color:#fff; }
.bk-actions .edit{ color:var(--ink); }
.bk-actions .edit:hover{ background:var(--ink); color:#fff; }
.bk-actions .delete{ color:#999; }
.bk-actions .delete:hover{ background:#999; color:#fff; }
.tag.pending{ background:rgba(192,138,46,.18); color:#8a611f; }
.tag.confirmed{ background:rgba(63,125,83,.18); color:#2c5a3a; }
.tag.cancelled{ background:rgba(153,153,153,.2); color:#666; text-decoration:line-through; }
.admin-empty{ padding:30px 10px; text-align:center; color:var(--ink-soft); font-size:.88rem; }
.admin-add-btn{ margin-top:20px; }
@media (max-width:640px){
  .booking-card{ flex-direction:column; }
  .bk-actions{ width:100%; }
}
.hidden{ display:none !important; }

/* ---- Admin dashboard ---- */
.admin-dash-head{ display:flex; justify-content:space-between; align-items:flex-start; gap:20px; flex-wrap:wrap; }
.admin-dash-head p{ max-width:560px; margin-top:6px; }
.admin-tabs{ display:flex; gap:10px; margin:26px 0 20px; border-bottom:1px solid var(--line); }
.admin-tab{ background:none; border:none; padding:12px 6px; font-family:'Jost',sans-serif; font-size:.86rem; font-weight:600; letter-spacing:.03em; color:var(--ink-soft); cursor:pointer; border-bottom:2px solid transparent; display:flex; align-items:center; gap:8px; }
.admin-tab.active{ color:var(--green-900); border-bottom-color:var(--maroon); }
.tab-count{ background:var(--green-900); color:var(--gold-light); font-size:.68rem; padding:2px 8px; border-radius:20px; }
.admin-tab.active .tab-count{ background:var(--maroon); color:#fff; }
.admin-tab-panel.hidden{ display:none; }
.admin-filter{ display:flex; gap:8px; margin-bottom:18px; flex-wrap:wrap; }
.filter-chip{ background:#fff; border:1px solid var(--line); padding:8px 16px; border-radius:20px; font-size:.76rem; letter-spacing:.03em; text-transform:uppercase; cursor:pointer; color:var(--ink-soft); transition:all .25s; }
.filter-chip.active{ background:var(--green-900); color:var(--gold-light); border-color:var(--green-900); }

.data-table{ display:flex; flex-direction:column; gap:10px; }
.d-row{ display:grid; grid-template-columns:110px 90px 1.3fr 1fr 100px auto; gap:14px; align-items:center; background:#fff; border:1px solid var(--line); padding:14px 16px; font-size:.84rem; }
.d-row .d-date{ font-family:'Marcellus',serif; color:var(--green-900); }
.d-row .d-slot{ font-size:.72rem; text-transform:uppercase; letter-spacing:.06em; color:var(--ink-soft); }
.d-row .d-cust b{ display:block; color:var(--ink); }
.d-row .d-cust span{ font-size:.78rem; color:var(--ink-soft); }
.d-row .d-type{ color:var(--ink-soft); }
.d-row .d-actions{ display:flex; gap:6px; flex-wrap:wrap; justify-content:flex-end; }
.d-row .d-actions button{ background:none; border:1px solid var(--line); padding:6px 10px; font-size:.7rem; text-transform:uppercase; letter-spacing:.04em; cursor:pointer; border-radius:2px; transition:all .2s; }
.d-row .d-actions button.confirm{ color:var(--ok); border-color:var(--ok); }
.d-row .d-actions button.confirm:hover{ background:var(--ok); color:#fff; }
.d-row .d-actions button.cancel{ color:var(--danger); border-color:var(--danger); }
.d-row .d-actions button.cancel:hover{ background:var(--danger); color:#fff; }
.d-row .d-actions button.edit{ color:var(--ink); }
.d-row .d-actions button.edit:hover{ background:var(--ink); color:#fff; }
.d-row .d-actions button.delete{ color:var(--ink-soft); }
.d-row .d-actions button.delete:hover{ background:var(--ink-soft); color:#fff; }
.status-badge{ font-size:.66rem; letter-spacing:.08em; text-transform:uppercase; padding:5px 10px; border-radius:20px; font-weight:600; text-align:center; }
.status-badge.pending{ background:rgba(192,138,46,.16); color:#8a5f1c; }
.status-badge.confirmed{ background:rgba(63,125,83,.16); color:#2c5a3a; }
.status-badge.cancelled{ background:rgba(178,58,58,.14); color:#8f2e2e; }
.status-badge.new{ background:rgba(124,31,43,.14); color:var(--maroon); }
.status-badge.contacted{ background:rgba(192,138,46,.16); color:#8a5f1c; }
.status-badge.closed{ background:#e7e2d3; color:var(--ink-soft); }
.empty-state{ text-align:center; padding:40px 20px; color:var(--ink-soft); font-size:.9rem; border:1px dashed var(--line); background:#fff; }

/* enquiry rows: different column layout */
.e-row{ display:grid; grid-template-columns:1fr 1.3fr 1fr 110px auto; gap:14px; align-items:center; background:#fff; border:1px solid var(--line); padding:14px 16px; font-size:.84rem; }
.e-row .e-when{ font-size:.72rem; color:var(--ink-soft); }
.e-row .e-cust b{ display:block; color:var(--ink); }
.e-row .e-cust span{ font-size:.78rem; color:var(--ink-soft); }
.e-row .e-msg{ color:var(--ink-soft); font-size:.8rem; max-height:3.2em; overflow:hidden; }
.e-row select{ border:1px solid var(--line); padding:6px 8px; font-size:.72rem; border-radius:2px; }
.e-row .d-actions{ display:flex; gap:6px; justify-content:flex-end; }
.e-row .d-actions button{ background:none; border:1px solid var(--line); padding:6px 10px; font-size:.7rem; cursor:pointer; border-radius:2px; color:var(--ink-soft); }
.e-row .d-actions button:hover{ background:var(--ink-soft); color:#fff; }

@media (max-width:780px){
  .d-row{ grid-template-columns:1fr 1fr; row-gap:8px; }
  .d-row .d-actions{ grid-column:1 / -1; justify-content:flex-start; }
  .e-row{ grid-template-columns:1fr 1fr; row-gap:8px; }
  .e-row .d-actions{ grid-column:1 / -1; justify-content:flex-start; }
  .admin-dash-head{ flex-direction:column; }
}

/* ---------- Responsive ---------- */
@media (max-width:980px){
  .nav-links, .nav-cta .btn.btn-outline{ display:none; }
  .burger{ display:flex; }
  .about-grid{ grid-template-columns:1fr; gap:56px; }
  .photo-slot{ height:360px; }
  .h-grid{ grid-template-columns:repeat(2,1fr); }
  .am-cols{ grid-template-columns:1fr; gap:34px; }
  .g-grid{ grid-template-columns:repeat(3,1fr); grid-auto-rows:140px; }
  .loc-grid, .c-grid{ grid-template-columns:1fr; }
  .loc-map iframe{ height:340px; }
  .c-form{ border-right:1px solid var(--line); border-bottom:none; padding:44px 30px; }
  .c-side{ padding:44px 30px; }
  .footer-top{ grid-template-columns:1fr 1fr; }
  .booking-layout{ grid-template-columns:1fr; }
  .slot-panel{ position:static; }
  .admin-row{ grid-template-columns:1fr 1fr; }
}
@media (max-width:640px){
  .wrap{ padding:0 18px; }
  .hero-inner{ padding:150px 18px 110px; }
  .hero-btns{ flex-direction:column; width:100%; }
  .hero-btns .btn{ width:100%; }
  .h-grid{ grid-template-columns:1fr; }
  .g-grid{ grid-template-columns:repeat(2,1fr); grid-auto-rows:130px; }
  .g-item.wide{ grid-column:span 2; }
  .g-item.tall{ grid-row:span 1; }
  .field-row{ grid-template-columns:1fr; }
  .footer-top{ grid-template-columns:1fr 1fr; gap:34px 24px; padding:56px 0 40px; }
  .about-badge{ left:0; bottom:-24px; padding:16px 20px; }
  .about-media .frame{ display:none; }
  .float-stack{ right:14px; bottom:84px; }
  .day-cell .dnum{ font-size:.85rem; }
  .cal-legend{ gap:10px 16px; }
  .admin-row{ grid-template-columns:1fr; }
  .mobile-bar{
    display:flex; position:fixed; bottom:0; left:0; right:0; z-index:250; background:var(--green-900);
    border-top:1px solid rgba(198,161,91,.3);
  }
  .mobile-bar a{ flex:1; text-align:center; padding:14px 8px; font-size:.78rem; letter-spacing:.04em; text-transform:uppercase; font-weight:600; display:flex; flex-direction:column; align-items:center; gap:4px; color:var(--ivory); }
  .mobile-bar a svg{ width:19px; height:19px; }
  .mobile-bar a.call{ color:var(--gold-light); border-right:1px solid rgba(198,161,91,.25); }
  body{ padding-bottom:64px; }
}
@media (prefers-reduced-motion: reduce){
  *{ animation:none !important; transition:none !important; }
  .reveal{ opacity:1; transform:none; }
}
